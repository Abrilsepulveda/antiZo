import React, { component } from 'react'
import { Text , Stylesheet , View } from "react-native"

export default function EmpresaAdd() {
    ruturn(
        <view>
            <text> login empresas </text>
        </view>
    );
}
const styles = StyleSheet.create({});