import React, { component } from 'react'
import { Text , Stylesheet , View } from "react-native"

export default function UsuarioAdd() {
    ruturn(
        <view>
            <text> login usuarios </text>
        </view>
    );
}
const styles = StyleSheet.create({});